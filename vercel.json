{
  "version": 2,
  "builds": [
    {
      "src": "dashboard/**/*",
      "use": "@vercel/next",
      "config": {
        "distDir": "dashboard/.next"
      }
    },
    {
      "src": "backend/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/api/index.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    },
    {
      "src": "/(.*)",
      "dest": "dashboard/$1"
    }
  ]
}
